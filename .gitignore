*.iml
.gradle
.idea
.DS_Store
build
captures
.externalNativeBuild
.cxx
local.properties
xcuserdata
.kotlin
#So we don't accidentally commit our private keys
*.gpg
#Local MCP / agent-channel config (holds Firebase creds & UIDs — never commit)
.mcp.json